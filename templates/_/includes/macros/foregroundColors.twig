{% set foregroundColors = {
    'heading' : {
        'white'        :  'text-cyprus-500',
        'black'        :  'text-white',
        'green'        :  'text-white',
        'cyprus'       :  'text-white',
    },
    'eyebrow' : {
        'white'        :  'text-cyprus-500',
        'black'        :  'text-white',
        'green'        :  'text-white',
        'cyprus'       :  'text-green-500',
    }
} %}
{% if type is iterable %}
    {% for item in type %}
        {{ foregroundColors[item][background] ?? null }}
    {% endfor %}
{% else %}
    {{ foregroundColors[type][background] ?? null }}
{% endif %}

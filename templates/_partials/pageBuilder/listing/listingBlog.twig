{#
=====================================================================
    Blog (blog)

    Variables
        - Block (block)

    Fields:
        Content:
            - Related Blog (relatedBlog)

        Settings:
            - Limit (limit)
            - Listing Style (listingStyle)
            - Per Row Limit (perRowLimit)
            - Pagination? (pagination)
=====================================================================
#}
{{ craft.app.config.general.devMode ? ('<!-- Template: ' ~ _self ~ ' -->') | raw }}
{% import "_/includes/macros" as render %}

{% set blogListingStyle = block.listingStyle2.value|default('cards') %}

{% include "_partials/pageBuilder/listing/blogListingStyle/#{blogListingStyle}" %}

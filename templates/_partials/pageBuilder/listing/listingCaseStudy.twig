{#
=====================================================================
    Case Study (caseStudy)

    Variables
        - Block (block)

    Fields:
        Content:
            - Related Case Study (relatedCaseStudy)

        Settings:
            - Case Study Layout (caseStudyLayout)
            - Limit (limit)
            - Listing Style (listingStyle2)
            - Per Row Limit (perRowLimit)
            - Pagination? (pagination)
=====================================================================
#}
{{ craft.app.config.general.devMode ? ('<!-- Template: ' ~ _self ~ ' -->') | raw }}
{% import "_/includes/macros" as render %}

{% set caseStudyLayout = block.caseStudyLayout.value|default('layout1') %}
{% set listingStyle = block.listingStyle2.value|default('cards') %}

{% include "_partials/pageBuilder/listing/caseStudyLayout/#{caseStudyLayout}/#{listingStyle}" %}
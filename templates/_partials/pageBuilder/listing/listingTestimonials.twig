{#
=====================================================================
    Testimonials (Testimonials)

    Variables
        - Block (block)

    Fields:
        Content:
            - Related Testimonials (relatedTestimonials)

        Settings:
            - Limit (limit)
=====================================================================
#}
{{ craft.app.config.general.devMode ? ('<!-- Template: ' ~ _self ~ ' -->') | raw }}
{% import "_/includes/macros" as render %}

{% set testimonialLayout = block.testimonialLayout.value|default('layout1') %}
{% set listingStyle = block.listingStyle.value|default('cards') %}

{% include "_partials/pageBuilder/listing/testimonialLayout/#{testimonialLayout}/#{listingStyle}" %}